[build-system]
requires = ["setuptools>=64", "wheel", "pybind11>=2.12.0"]
build-backend = "setuptools.build_meta"

[project]
name = "testbed"
version = "0.0.1"
description = "Python dashboard for the testbed"
authors = [
  { name="Kuravi H", email="kuravih@gmail.com" },
]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.8"
dependencies = ["toml","numpy","astropy","pyshmio", "pyzmq"]